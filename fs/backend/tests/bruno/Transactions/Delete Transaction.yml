info:
  name: Delete Transaction
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/transactions/{{transactionId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
