info:
  name: Delete Category
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/categories/{{categoryId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
