info:
  name: List Categories
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/categories?kind=expense"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
