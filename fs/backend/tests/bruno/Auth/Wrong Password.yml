info:
  name: Wrong Password
  type: http
  seq: 2

http:
  method: POST
  url: '{{baseUrl}}/authentications'
  body:
    type: json
    data: |-
      {
        "email": "makise@amadeus.com",
        "password": "wrongpassword"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
