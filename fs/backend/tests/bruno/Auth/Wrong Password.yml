info:
  name: Wrong Password
  type: http
  seq: 2

http:
  method: POST
  url: '{{baseUrl}}/api/v1/auth/login'
  body:
    type: json
    data: |-
      {
        "email": "makise@amadeus.com",
        "password": "wrongpassword"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
