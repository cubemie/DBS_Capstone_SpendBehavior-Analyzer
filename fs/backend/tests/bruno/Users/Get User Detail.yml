info:
  name: Get User Detail
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/users/{{loggedInUserId}}"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
