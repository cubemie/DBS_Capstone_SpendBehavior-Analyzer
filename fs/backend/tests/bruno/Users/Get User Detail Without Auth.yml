info:
  name: Get User Detail Without Auth
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/users/{{loggedInUserId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
